

# Add "Back to Website" Button to Auth Page

Add a "Back to Website" link below the card on the Auth page (`src/pages/Auth.tsx`), matching the pattern used on the Child Login page.

**Change**: After the closing `</Card>` tag (line 278), add a small text link navigating to `/` with a Home icon, styled as `ghost` variant button.

